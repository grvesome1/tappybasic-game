# Changelog

## v2.4.1 — 2026-01-07
- Start “Arcade Integration Kit” refactor: standardized deployment manifest naming (`deployments/arcade.*.json` + `deployments/latest.json`).
- Fix EIP-712 domain version drift (router is `2.4`).
- Prepare repo for SDK + backend kit packages (workspaces to follow).

## v2.4.0
- Initial v2.4 deployment package.
